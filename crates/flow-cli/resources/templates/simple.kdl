// シンプルな例: PostgreSQLのみ

stage "local" {
    service "postgres"
    variables {
        LOG_LEVEL "debug"
    }
}

service "postgres" {
    version "16"
    port 5432:5432
    environment {
        POSTGRES_PASSWORD "postgres"
    }
}
