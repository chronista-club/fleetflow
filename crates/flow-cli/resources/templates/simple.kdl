// シンプルな例: PostgreSQLのみ

stage "local" {
    service "postgres"
    variables {
        LOG_LEVEL "debug"
    }
}

service "postgres" {
    version "16"
    ports {
        port host=11432 container=5432
    }
    environment {
        POSTGRES_PASSWORD "postgres"
    }
}
